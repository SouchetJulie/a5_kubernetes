apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx

spec:
  selector:
    app: nginx # targets the pods with this label
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
